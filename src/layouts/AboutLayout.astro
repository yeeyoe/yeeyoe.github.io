---
import PageLayout from "./PageLayout.astro";

export interface Props {
  frontmatter: {
    title: string;
    description?: string;
  };
}

const { frontmatter } = Astro.props;
---

<PageLayout frontmatter={frontmatter} pageTitleKey="about">
  <slot />
</PageLayout>
